/**
 * Disaster Authority Command & Verification Console (AdminConsole)
 * Multi-modal AI Vision Inspection, Verification Queue & Alert Broadcast Gateway.
 */

import { AdminDashboard } from './AdminDashboard';

export const AdminConsole = AdminDashboard;
export default AdminDashboard;
